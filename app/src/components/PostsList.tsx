import styled from "styled-components";

const PostsWrapper = styled.div`
  width: 100%;
  height: 100%;
  margin: 48px 64px 0 24px;
`;

export const PostsList = () => {
  return <PostsWrapper></PostsWrapper>;
};
